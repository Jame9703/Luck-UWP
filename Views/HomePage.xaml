<Page
    x:Class="Luck.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Luck"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Red">
        <ScrollViewer x:Name="scrollViewerForAnimation" VerticalScrollBarVisibility="Auto" Height="100">
            <StackPanel x:Name="animatedStackPanel" Orientation="Vertical" Height="100">
                <TextBlock Text="张三" FontSize="18" />
                <TextBlock Text="李四" FontSize="18" />
                <TextBlock Text="王五" FontSize="18" />
                <!-- 添加更多文本块表示名单内容 -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
